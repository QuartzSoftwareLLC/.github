name: Echoer

on:
  push:

jobs:
  echo-template-action:
    runs-on: self-hosted
    steps:
    - name: echo
      uses: quartzsoftwarellc/.github/actions/echo.yaml@main
      with:
        message: "hello"
  echo-workflow:
    uses: quartzsoftwarellc/.github/workflows/echo.yaml@main
